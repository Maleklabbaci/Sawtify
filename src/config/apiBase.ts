// URL du backend (Render en production, ex: https://sawtify-api.onrender.com).
// Vide = même domaine (dev local, ou si un jour tout est sur un seul serveur).
export const API_BASE_URL = import.meta.env.VITE_API_URL || '';
